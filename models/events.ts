export enum SocketEvents {
    CONNECT = "connect",
    DISCONNECT = "disconnect",

    JOIN = "user-join",
    LEAVE = "user-leave",

    CLAIM_PIXEL = "claim-pixel",
    UPDATE_PIXEL = "update-pixel"
}
